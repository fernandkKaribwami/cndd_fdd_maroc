[phases.build]
cmds = ["pip install -r requirements.txt", "python manage.py collectstatic --noinput"]

[start]
cmd = "python -m gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 1 --timeout 120"
